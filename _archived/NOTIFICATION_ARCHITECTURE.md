# Incident Notification System - Architecture & Flow

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                       MOBILE APP (React Native)                  │
│                                                                   │
│  ┌──────────────────┐        ┌──────────────────┐              │
│  │ Report Incident  │        │  Notifications   │              │
│  │   Screen         │        │     Screen       │              │
│  │                  │        │                  │              │
│  │ [Submit Report]  │───┐    │ • Unread Badge   │              │
│  │                  │   │    │ • Notification   │              │
│  │ (Authenticated)  │   │    │   List           │              │
│  └──────────────────┘   │    │ • Mark as Read   │              │
│                         │    │ • Tap → Details  │              │
│                         │    └────────┬──────────┘              │
│                         │             │                         │
│                         │             │                         │
│  ┌──────────────────┐  │             │                         │
│  │  My Reports      │◄─┴─────────────┘                         │
│  │   Screen         │                                           │
│  │                  │                                           │
│  │ • Report List    │                                           │
│  │ • Report Detail  │                                           │
│  │ • Timeline       │                                           │
│  └──────────────────┘                                           │
│                                                                   │
└───────────────────────┬───────────────────────────────────────┘
                        │
                        │ HTTP/REST API
                        │ (JWT Auth)
                        │
┌───────────────────────▼───────────────────────────────────────┐
│                    BACKEND (Node.js/Express)                   │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │                    API Routes Layer                       │ │
│  │                                                            │ │
│  │  ┌─────────────────┐      ┌──────────────────┐          │ │
│  │  │ /api/incidents  │      │ /api/notifications│          │ │
│  │  │                 │      │                   │          │ │
│  │  │ POST   (create) │──┐   │ GET    (list)     │          │ │
│  │  │ PUT    (update) │  │   │ PUT    (mark read)│          │ │
│  │  │ GET    (detail) │  │   │ DELETE (delete)   │          │ │
│  │  └─────────────────┘  │   └──────────────────┘          │ │
│  └─────────────────────────┼──────────────────────────────┘ │
│                            │                                  │
│                            ▼                                  │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │              Notification Service Layer                   │ │
│  │                                                            │ │
│  │  • createIncidentNotification()                          │ │
│  │  • notifyIncidentSubmitted()                             │ │
│  │  • notifyIncidentStatusChange()                          │ │
│  │                                                            │ │
│  │  Logic:                                                   │ │
│  │  ✓ Check if user is authenticated pet owner             │ │
│  │  ✓ Determine notification type & message                │ │
│  │  ✓ Create notification record                           │ │
│  │  ✓ Include rejection reason (if applicable)             │ │
│  └───────────────────────┬──────────────────────────────────┘ │
│                          │                                     │
└──────────────────────────┼─────────────────────────────────────┘
                           │
                           ▼
┌───────────────────────────────────────────────────────────────┐
│                   DATABASE (MySQL)                             │
│                                                                 │
│  ┌──────────────────────┐       ┌──────────────────────┐      │
│  │ incident_report      │       │  notifications       │      │
│  │                      │       │                      │      │
│  │ • report_id (PK)     │       │ • notification_id    │      │
│  │ • owner_id (FK)      │◄──────┤ • owner_id (FK)      │      │
│  │ • reporter_id        │       │ • incident_id (FK)   │      │
│  │ • status             │       │ • title              │      │
│  │ • report_type        │       │ • message            │      │
│  │ • description        │       │ • type               │      │
│  │ • location_id        │       │ • is_read            │      │
│  │ • created_at         │       │ • created_at         │      │
│  └──────────────────────┘       └──────────────────────┘      │
│                                                                 │
│  ┌──────────────────────┐                                      │
│  │ pet_owner            │                                      │
│  │                      │                                      │
│  │ • owner_id (PK)      │                                      │
│  │ • full_name          │                                      │
│  │ • email              │                                      │
│  │ • password           │                                      │
│  └──────────────────────┘                                      │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📊 Notification Flow Diagrams

### Flow 1: Incident Submission → Notification

```
┌──────────────┐
│ Pet Owner    │
│ (Mobile App) │
└───────┬──────┘
        │
        │ 1. Submit Incident Report
        │    (Authenticated)
        ▼
┌───────────────────────┐
│ POST /api/incidents   │
│                       │
│ • Validates JWT token │
│ • Extracts user info  │
│ • Creates incident    │
└───────┬───────────────┘
        │
        │ 2. After incident created
        │    Check: req.user.userType === 'pet_owner'
        ▼
┌───────────────────────────────┐
│ notifyIncidentSubmitted()     │
│                               │
│ • ownerId: req.user.id        │
│ • incidentId: new incident ID │
│ • type: "submission"          │
└───────┬───────────────────────┘
        │
        │ 3. Insert into database
        ▼
┌───────────────────────────────┐
│ INSERT INTO notifications     │
│                               │
│ title: "Incident Report       │
│         Submitted"            │
│ message: "Your incident       │
│          report was           │
│          submitted..."        │
└───────┬───────────────────────┘
        │
        │ 4. Return success
        ▼
┌──────────────┐
│ Mobile App   │
│              │
│ • Polls API  │
│ • Sees new   │
│   notification│
│ • Shows badge│
└──────────────┘
```

### Flow 2: Status Update → Notification

```
┌──────────────┐
│ Admin        │
│ (Web Portal) │
└───────┬──────┘
        │
        │ 1. Update incident status
        │    (e.g., "In Progress")
        ▼
┌───────────────────────┐
│ PUT /api/incidents/:id│
│                       │
│ Body: {               │
│   status: "In Progress"│
│ }                     │
└───────┬───────────────┘
        │
        │ 2. Fetch current incident
        │    Check: incident.owner_id exists?
        ▼
┌───────────────────────────────┐
│ Incident.getById(id)          │
│                               │
│ Returns:                      │
│ • owner_id: 123               │
│ • current status: "Verified"  │
└───────┬───────────────────────┘
        │
        │ 3. Detect status change
        │    old: "Verified" → new: "In Progress"
        ▼
┌───────────────────────────────┐
│ notifyIncidentStatusChange()  │
│                               │
│ • ownerId: 123                │
│ • incidentId: 456             │
│ • status: "In Progress"       │
│ • rejectionReason: null       │
└───────┬───────────────────────┘
        │
        │ 4. Map status to message
        ▼
┌───────────────────────────────┐
│ Status Mapping:               │
│                               │
│ "In Progress" →               │
│   title: "Patrol In Progress" │
│   message: "A patrol team..." │
│   type: "status_update"       │
└───────┬───────────────────────┘
        │
        │ 5. Insert notification
        ▼
┌───────────────────────────────┐
│ INSERT INTO notifications     │
└───────┬───────────────────────┘
        │
        │ 6. Pet owner sees it
        ▼
┌──────────────┐
│ Mobile App   │
│              │
│ • Polls API  │
│ • New notif  │
│   appears    │
└──────────────┘
```

### Flow 3: Notification Tap → Navigation

```
┌──────────────┐
│ Pet Owner    │
│ (Mobile App) │
└───────┬──────┘
        │
        │ 1. Tap notification
        ▼
┌─────────────────────────────────┐
│ NotificationScreen.openDetail() │
│                                 │
│ • Mark as read (API call)       │
│ • Check notification type       │
└───────┬─────────────────────────┘
        │
        │ 2. Is incident notification?
        │    (type: submission/status_update/rejection)
        ▼
┌─────────────────────────────────┐
│ PUT /api/notifications/:id/read │
│                                 │
│ Response: { success: true }     │
└───────┬─────────────────────────┘
        │
        │ 3. Navigate with params
        ▼
┌─────────────────────────────────┐
│ navigation.navigate('MyReports',│
│   { highlightIncidentId: 456 } )│
└───────┬─────────────────────────┘
        │
        │ 4. MyReportsScreen receives param
        ▼
┌─────────────────────────────────┐
│ MyReportsScreen                 │
│                                 │
│ useFocusEffect(() => {          │
│   if (highlightIncidentId) {    │
│     navigate('ReportDetail',    │
│       { reportId: 456 })        │
│   }                             │
│ })                              │
└───────┬─────────────────────────┘
        │
        │ 5. Show incident details
        ▼
┌─────────────────────────────────┐
│ ReportDetailScreen              │
│                                 │
│ • Incident info                 │
│ • Timeline                      │
│ • Status                        │
│ • Patrol details                │
│ • Images                        │
└─────────────────────────────────┘
```

---

## 🔄 Status Lifecycle with Notifications

```
┌─────────────────────────────────────────────────────────────────┐
│                    INCIDENT LIFECYCLE                            │
└─────────────────────────────────────────────────────────────────┘

    ┌──────────────┐
    │   SUBMIT     │  ← Pet Owner submits via mobile app
    │   (Auth)     │
    └──────┬───────┘
           │
           │ ✉️ Notification: "Incident Report Submitted"
           │    Type: submission
           ▼
    ┌──────────────┐
    │   PENDING    │  ← Initial status (waiting for admin)
    │              │
    └──────┬───────┘
           │
           │ Admin reviews
           │
     ┌─────┴──────┐
     │            │
     ▼            ▼
┌─────────┐  ┌─────────┐
│VERIFIED │  │REJECTED │
│         │  │         │
└────┬────┘  └────┬────┘
     │            │
     │            │ ✉️ Notification: "Report Rejected"
     │            │    Type: rejection
     │            │    Message includes: rejection_reason
     │            │
     │            ▼
     │       ┌────────┐
     │       │  END   │
     │       └────────┘
     │
     │ ✉️ Notification: "Report Verified"
     │    Type: status_update
     ▼
┌──────────────┐
│  SCHEDULED   │  ← Patrol team assigned
│              │
└──────┬───────┘
       │
       │ ✉️ Notification: "Report Verified and Scheduled"
       │    Type: status_update
       ▼
┌──────────────┐
│ IN PROGRESS  │  ← Patrol team on site
│              │
└──────┬───────┘
       │
       │ ✉️ Notification: "Patrol In Progress"
       │    Type: status_update
       ▼
┌──────────────┐
│  RESOLVED    │  ← Incident handled successfully
│              │
└──────────────┘
       │
       │ ✉️ Notification: "Report Resolved"
       │    Type: status_update
       ▼
    ┌────────┐
    │  END   │
    └────────┘
```

---

## 🗂️ Data Relationships

```
pet_owner                notifications            incident_report
┌────────────┐          ┌──────────────┐         ┌───────────────┐
│ owner_id ━━━━━━━━━━━━━▶ owner_id     │         │ report_id     │
│ full_name  │          │              │◀━━━━━━━━│ owner_id      │
│ email      │          │ incident_id ━━━━━━━━━━▶│ status        │
│ password   │          │              │         │ report_type   │
└────────────┘          │ title        │         │ description   │
                        │ message      │         └───────────────┘
                        │ type         │
                        │ is_read      │
                        │ created_at   │
                        └──────────────┘

Key:
━━▶ Foreign Key Relationship
```

---

## 🔐 Authentication Flow

```
┌──────────────┐
│ Mobile App   │
│              │
│ Login Screen │
└──────┬───────┘
       │
       │ 1. POST /api/auth/login
       │    { email, password, userType: "pet_owner" }
       ▼
┌────────────────────┐
│ Backend Auth       │
│                    │
│ • Validate creds   │
│ • Generate JWT     │
│ • Return token     │
└────────┬───────────┘
         │
         │ 2. Response: { token, userId, userType }
         ▼
┌────────────────────┐
│ Mobile App         │
│                    │
│ • Store token      │
│   in AsyncStorage  │
│ • Store user data  │
└────────┬───────────┘
         │
         │ 3. All subsequent requests
         │    Header: Authorization: Bearer <token>
         ▼
┌────────────────────┐
│ Backend            │
│ authenticateToken  │
│ middleware         │
│                    │
│ • Verify JWT       │
│ • Extract user     │
│ • Set req.user     │
└────────┬───────────┘
         │
         │ 4. req.user available in routes
         │    { id: 123, userType: "pet_owner", ... }
         ▼
┌────────────────────┐
│ Route Handler      │
│                    │
│ if (req.user) {    │
│   // Authenticated │
│   // Create notif  │
│ }                  │
└────────────────────┘
```

---

## 📱 Mobile UI State Flow

```
┌───────────────────────────────────────────────────────────┐
│                  NotificationScreen                        │
└───────────────────────────────────────────────────────────┘

Initial Load:
  • fetchNotifications() called
  • Polls API every 10 seconds
  • Updates state: setNotifications()

User Actions:
  ┌─────────────────────────────────────────────────────────┐
  │ 1. Pull to Refresh                                       │
  │    → setRefreshing(true)                                │
  │    → fetchNotifications()                               │
  │    → setRefreshing(false)                               │
  └─────────────────────────────────────────────────────────┘
  
  ┌─────────────────────────────────────────────────────────┐
  │ 2. Tap Notification                                      │
  │    → openDetail(notification)                           │
  │    → api.notifications.markRead(id)                     │
  │    → Update local state (set read: true)                │
  │    → Navigate to MyReports                              │
  └─────────────────────────────────────────────────────────┘
  
  ┌─────────────────────────────────────────────────────────┐
  │ 3. Mark All as Read                                      │
  │    → setMarkingAll(true)                                │
  │    → api.notifications.markAllRead()                    │
  │    → fetchNotifications() (refresh list)                │
  │    → setMarkingAll(false)                               │
  └─────────────────────────────────────────────────────────┘
  
  ┌─────────────────────────────────────────────────────────┐
  │ 4. Swipe to Delete                                       │
  │    → deleteNotification(id)                             │
  │    → api.notifications.delete(id)                       │
  │    → Remove from local state                            │
  │    → fetchUnread() (update count)                       │
  └─────────────────────────────────────────────────────────┘

State Variables:
  • notifications: []
  • loading: boolean
  • refreshing: boolean
  • unreadCount: number
  • selectedFilter: string
  • searchQuery: string
```

---

## 🎯 Key Design Decisions

### 1. Server-Side Generation
**Decision:** All notifications generated on backend  
**Rationale:** Ensures consistency, security, and single source of truth

### 2. Polling vs. WebSocket
**Decision:** 10-second polling  
**Rationale:** Simpler implementation, adequate for use case, easier to maintain

### 3. Database Schema Extension
**Decision:** Add `owner_id` and `incident_id` to existing table  
**Rationale:** No new tables needed, maintains simplicity, backward compatible

### 4. Navigation Pattern
**Decision:** Notification → MyReports → ReportDetail  
**Rationale:** Follows app's existing navigation structure, maintains context

### 5. Error Handling
**Decision:** Notification failures don't break incident creation  
**Rationale:** Primary operation (incident) more important than side effect (notification)

---

This architecture ensures a robust, maintainable, and user-friendly notification system.
