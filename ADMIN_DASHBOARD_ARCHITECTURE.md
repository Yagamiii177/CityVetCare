# 🏗️ Admin Dashboard - System Architecture

## 📐 Component Hierarchy

```
┌─────────────────────────────────────────────────────────────────┐
│                      AdminDashboard.jsx                         │
│                    (Main Container)                             │
├─────────────────────────────────────────────────────────────────┤
│  ┌──────────────┐  Auto-Refresh: 30s                           │
│  │   Header     │  Error Handling                              │
│  │   + Drawer   │  Loading States                              │
│  └──────────────┘  Data Orchestration                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │          KEY METRICS (Top Row - 5 Cards)                │  │
│  ├─────────────────────────────────────────────────────────┤  │
│  │ Total │ Pending │ Approved │ Rejected │ Inactive        │  │
│  │  45   │   8     │    32    │    3     │    2            │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │              ALERTS PANEL (If alerts exist)             │  │
│  ├─────────────────────────────────────────────────────────┤  │
│  │ ⚠️  3 clinics pending for more than 7 days             │  │
│  │ ℹ️  5 clinics missing GPS coordinates                  │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ┌───────────────────────────┬─────────────────────────────┐  │
│  │  LEFT COLUMN (2/3)        │  RIGHT COLUMN (1/3)         │  │
│  ├───────────────────────────┼─────────────────────────────┤  │
│  │                           │                             │  │
│  │  ┌──────────────────────┐ │  ┌────────────────────────┐│  │
│  │  │ APPROVAL CENTER      │ │  │  MINI CLINIC MAP       ││  │
│  │  ├──────────────────────┤ │  ├────────────────────────┤│  │
│  │  │ 📋 Pending Clinics   │ │  │  🗺️  [Interactive Map] ││  │
│  │  │  • Clinic A (8 days) │ │  │  • Active: 32          ││  │
│  │  │    [Approve][Reject] │ │  │  • Pending: 8          ││  │
│  │  │  • Clinic B (5 days) │ │  │  [View Full Map]       ││  │
│  │  │    [Approve][Reject] │ │  └────────────────────────┘│  │
│  │  │  • Clinic C (2 days) │ │                             │  │
│  │  │    [Approve][Reject] │ │  ┌────────────────────────┐│  │
│  │  │  [View All →]        │ │  │  QUICK ACTIONS         ││  │
│  │  └──────────────────────┘ │  ├────────────────────────┤│  │
│  │                           │  │  📋 Review Pending     ││  │
│  │  ┌──────────────────────┐ │  │  🏥 View Clinic List   ││  │
│  │  │ ANALYTICS & TRENDS   │ │  │  🗺️  Open Map          ││  │
│  │  ├──────────────────────┤ │  │  📊 View Statistics    ││  │
│  │  │ [Metrics Summary]    │ │  └────────────────────────┘│  │
│  │  │ 📈 Registration      │ │                             │  │
│  │  │    Trends Chart      │ │  ┌────────────────────────┐│  │
│  │  │ 🥧 Status Pie Chart  │ │  │  SYSTEM STATUS         ││  │
│  │  │ 📊 Top Barangays     │ │  ├────────────────────────┤│  │
│  │  │ [View Details →]     │ │  │  • Database: Online    ││  │
│  │  └──────────────────────┘ │  │  • API: Online         ││  │
│  │                           │  │  • Updated: 10:30 AM   ││  │
│  │  ┌──────────────────────┐ │  └────────────────────────┘│  │
│  │  │ ACTIVITY LOG         │ │                             │  │
│  │  ├──────────────────────┤ │                             │  │
│  │  │ ✅ Clinic A approved │ │                             │  │
│  │  │    2 hours ago       │ │                             │  │
│  │  │ 📝 Clinic B submitted│ │                             │  │
│  │  │    5 hours ago       │ │                             │  │
│  │  │ ❌ Clinic C rejected │ │                             │  │
│  │  │    1 day ago         │ │                             │  │
│  │  └──────────────────────┘ │                             │  │
│  └───────────────────────────┴─────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow Architecture

```
┌──────────────────────────────────────────────────────────────┐
│                    FRONTEND (React)                          │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  AdminDashboard.jsx                                          │
│       │                                                      │
│       ├─ fetchDashboardData() ────┐                        │
│       │                            │                         │
│       │    ┌──────────────────────┴────────────────────┐   │
│       │    │    apiService.adminDashboard              │   │
│       │    │    - getStats()                           │   │
│       │    │    - getPendingClinics()                  │   │
│       │    │    - getActivity()                        │   │
│       │    │    - getAnalytics()                       │   │
│       │    │    - getAlerts()                          │   │
│       │    │    - approveClinic(id)                    │   │
│       │    │    - rejectClinic(id, reason)             │   │
│       │    └──────────────┬────────────────────────────┘   │
│       │                   │                                 │
│       v                   v                                 │
│  ┌─────────────┐   ┌─────────────┐                        │
│  │  Components │   │  API Calls  │                         │
│  │  Render     │←──│  (axios)    │                         │
│  └─────────────┘   └──────┬──────┘                         │
└────────────────────────────┼──────────────────────────────┘
                             │
                             │ HTTP Requests
                             │ (JSON)
                             v
┌──────────────────────────────────────────────────────────────┐
│                    BACKEND (Node.js/Express)                 │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  server.js                                                   │
│    │                                                         │
│    ├─ /api/admin-dashboard/* ───► admin-dashboard.js        │
│    │                                    │                    │
│    │                            ┌───────┴──────────┐        │
│    │                            │  Route Handlers  │        │
│    │                            ├──────────────────┤        │
│    │                            │ GET /stats       │        │
│    │                            │ GET /pending     │        │
│    │                            │ GET /activity    │        │
│    │                            │ GET /analytics   │        │
│    │                            │ GET /alerts      │        │
│    │                            │ PATCH /approve   │        │
│    │                            │ PATCH /reject    │        │
│    │                            └─────┬────────────┘        │
│    │                                  │                     │
│    │                                  v                     │
│    │                         ┌────────────────┐            │
│    │                         │  SQL Queries   │            │
│    │                         └────────┬───────┘            │
└─────────────────────────────────────────┼───────────────────┘
                                          │
                                          v
┌──────────────────────────────────────────────────────────────┐
│                    DATABASE (MySQL)                          │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  private_clinic Table                                        │
│  ┌────────────────────────────────────────────────────┐    │
│  │ clinic_id, clinic_name, status, head_veterinarian  │    │
│  │ email, contact_number, address, barangay           │    │
│  │ latitude, longitude, license_number, services      │    │
│  │ date_created, date_updated, rejection_reason       │    │
│  └────────────────────────────────────────────────────┘    │
│                                                              │
│  Queries:                                                    │
│   • Aggregations (COUNT, SUM with CASE)                    │
│   • Time-based grouping (monthly trends)                   │
│   • Filtering (status, barangay, etc.)                     │
│   • Sorting (date_created, priority)                       │
│   • Updates (status, date_updated)                         │
└──────────────────────────────────────────────────────────────┘
```

---

## 🎯 Request/Response Flow

### Example: Loading Dashboard

```
1. USER opens /admin-dashboard
     ↓
2. AdminDashboard.jsx mounts
     ↓
3. useEffect() triggers fetchDashboardData()
     ↓
4. 6 Parallel API calls:
     ├─ GET /api/admin-dashboard/stats
     ├─ GET /api/admin-dashboard/pending-clinics?limit=10
     ├─ GET /api/admin-dashboard/activity?limit=20
     ├─ GET /api/admin-dashboard/analytics
     ├─ GET /api/admin-dashboard/alerts
     └─ GET /api/clinics/locations?status=all
     ↓
5. Backend receives requests → routes to handlers
     ↓
6. SQL queries execute in parallel
     ↓
7. Database returns results
     ↓
8. Backend formats as JSON responses
     ↓
9. Frontend receives data → updates state
     ↓
10. React components re-render with data
     ↓
11. USER sees populated dashboard
     ↓
12. Auto-refresh timer starts (30s)
```

### Example: Approving a Clinic

```
1. USER clicks "Approve" button on Clinic ID 123
     ↓
2. ApprovalCenter.jsx → handleApprove(123)
     ↓
3. setProcessingId(123) → button disabled
     ↓
4. apiService.adminDashboard.approveClinic(123)
     ↓
5. PATCH /api/admin-dashboard/clinics/123/approve
     ↓
6. Backend route handler executes
     ↓
7. SQL UPDATE:
    SET status = 'Active', date_updated = NOW()
    WHERE clinic_id = 123 AND status = 'Pending'
     ↓
8. Database confirms update (1 row affected)
     ↓
9. Backend returns:
    { success: true, message: "Clinic approved", data: {...} }
     ↓
10. Frontend onApprove() callback → fetchDashboardData()
     ↓
11. All dashboard data refreshes
     ↓
12. USER sees:
     • Pending count decreased
     • Approved count increased
     • Clinic removed from pending list
     • Activity log updated
     • Success feedback
```

---

## 🗂️ State Management

```javascript
AdminDashboard.jsx State:

┌─────────────────────────────────────┐
│  UI State                           │
├─────────────────────────────────────┤
│  • isDrawerOpen: boolean            │
│  • loading: boolean                 │
│  • error: string | null             │
│  • autoRefresh: boolean             │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│  Data State                         │
├─────────────────────────────────────┤
│  • metrics: {                       │
│      totalClinics: number,          │
│      pendingApprovals: number,      │
│      approvedClinics: number,       │
│      rejectedClinics: number,       │
│      inactiveClinics: number        │
│    }                                │
│  • pendingClinics: array            │
│  • recentActivity: array            │
│  • analyticsData: object            │
│  • alerts: array                    │
│  • clinicLocations: array           │
└─────────────────────────────────────┘
```

---

## 🎨 Component Props Flow

```
AdminDashboard
    │
    ├─► MetricsCard
    │     • title: string
    │     • value: number
    │     • icon: Component
    │     • color: string
    │     • urgent: boolean
    │     • onClick: function
    │
    ├─► AlertsPanel
    │     • alerts: array
    │     • onRefresh: function
    │
    ├─► ApprovalCenter
    │     • pendingClinics: array
    │     • onApprove: function
    │     • onReject: function
    │     • onViewAll: function
    │
    ├─► AnalyticsCharts
    │     • data: object
    │     • onViewDetails: function
    │
    ├─► ActivityLog
    │     • activities: array
    │     • loading: boolean
    │
    └─► MiniClinicMap
          • clinics: array
          • onViewFullMap: function
```

---

## 🔐 Security Architecture

```
┌────────────────────────────────────────────────┐
│           Security Layers                      │
├────────────────────────────────────────────────┤
│                                                │
│  1. Frontend (Browser)                         │
│     • Token storage (localStorage)             │
│     • Request interceptor (add token)          │
│     • Error handling (401 → logout)            │
│                                                │
│  2. Network (HTTPS in production)              │
│     • Encrypted transmission                   │
│     • CORS policy enforcement                  │
│                                                │
│  3. Backend (Express middleware)               │
│     • Token verification                       │
│     • Role checking (admin only)               │
│     • Input validation                         │
│     • Rate limiting                            │
│                                                │
│  4. Database                                   │
│     • Parameterized queries (SQL injection)    │
│     • Connection pooling                       │
│     • Access control                           │
│                                                │
└────────────────────────────────────────────────┘
```

---

## 📊 Performance Optimization

```
┌────────────────────────────────────────────────┐
│         Performance Strategies                 │
├────────────────────────────────────────────────┤
│                                                │
│  Frontend:                                     │
│    ✓ Parallel API calls                       │
│    ✓ React memo for components                │
│    ✓ Debounced search                         │
│    ✓ Lazy loading charts                      │
│    ✓ Optimized re-renders                     │
│                                                │
│  Backend:                                      │
│    ✓ Database connection pooling              │
│    ✓ Indexed columns (status, date_created)   │
│    ✓ Aggregated queries                       │
│    ✓ Response caching (consider Redis)        │
│    ✓ Pagination                                │
│                                                │
│  Database:                                     │
│    ✓ Compound indexes                         │
│    ✓ Query optimization                       │
│    ✓ Proper data types                        │
│    ✓ Regular ANALYZE TABLE                    │
│                                                │
└────────────────────────────────────────────────┘
```

---

## 🚀 Deployment Architecture

```
Production Environment:

┌──────────────────────────────────────────────┐
│            Load Balancer                     │
└────────────┬─────────────────────────────────┘
             │
    ┌────────┴────────┐
    │                 │
┌───▼────┐      ┌────▼───┐
│ Web    │      │ Web    │    (React App)
│ Server │      │ Server │    Nginx/Apache
└───┬────┘      └────┬───┘
    │                │
    └────────┬───────┘
             │
    ┌────────▼────────┐
    │   API Server    │      (Node.js)
    │   Backend       │      Express
    └────────┬────────┘
             │
    ┌────────▼────────┐
    │   Database      │      (MySQL)
    │   Cluster       │      Primary/Replica
    └─────────────────┘
```

---

## 📈 Scalability Considerations

1. **Horizontal Scaling:**

   - Multiple backend instances
   - Load balancer distribution
   - Stateless API design

2. **Database Optimization:**

   - Read replicas for analytics
   - Partitioning by date
   - Archiving old data

3. **Caching:**

   - Redis for statistics
   - CDN for static assets
   - Browser caching

4. **Monitoring:**
   - Error tracking (Sentry)
   - Performance monitoring
   - Database query analysis

---

**This architecture is production-ready and fully scalable!** 🚀
